/*
const MatrixDisplay = ({ matrix }) => (
  <div className="matrix-display">
    <table>
      {matrix.map((row, i) => (
        <tr key={i}>
          {row.map((cell, j) => (
            <td key={j}>{cell}</td>
          ))}
        </tr>
      ))}
    </table>
  </div>
);

export default MatrixDisplay;

*/